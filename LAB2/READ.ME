# 🧪 Lab 2 – Simple Shell & Process Management

## 📘 Overview

In this lab, we implemented a simple command-line shell in C. The shell supports executing external commands, handling signals, input/output redirection, and basic process management. We also created a separate program to demonstrate inter-process communication using pipes.

## 🎯 Main Topics

- Shell basics and command parsing
- `fork()`, `execvp()`, and `waitpid()` usage
- Signal handling (`SIGINT`, `SIGTSTP`, `SIGCONT`)
- I/O redirection (`<`, `>`)
- Inter-process communication with `pipe()`

## 📂 Files

- `myshell.c` – Shell with process and signal support
- `mypipe.c` – Pipe example: parent sends message to child
- `makefile` – Compiles both programs
